seams:
  - name: the launcher's verdict line
    at: plugins/bett3r-ai-workflow/bin/design-map:1
    kind: existing
ticket: ESAS-9
review: unattended
candidateOracles:
  - fork: ESAS-9-F1
    option: A
    scenario: the owner picks A
    source: owner
    example: A is applied
    slice: null
    status: unconfirmed
  - fork: ESAS-9-F2
    option: B
    scenario: the owner picks B
    source: recommendation
    example: B is applied
    slice: null
    status: unconfirmed
slices:
  - id: 1
    seam: the launcher's verdict line
    name: "slice one"
    oracle: |
      the launcher prints outcome=ok and exits 0
    scenarios:
      - scenario: the launcher runs over a conforming plan
        given: a plan whose candidates are all unconfirmed
        when: check-plan runs
        then: the launcher prints outcome=ok and exits 0
