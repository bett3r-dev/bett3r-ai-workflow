seams:
  - name: the launcher's verdict line
    at: plugins/bett3r-ai-workflow/bin/design-map:1
    kind: existing
ticket: ESAS-9
review: human
candidateOracles:
  - fork: ESAS-9-F1
    option: A
    scenario: the owner picks A
    source: owner
    example: A is applied
    slice: 1
    status: confirmed
slices:
  - id: 1
    probe: delete the line that wires the check into check_plan; the oracle must go red
    seam: the launcher's verdict line
    name: "slice one"
    oracle: |
      the fixture asserts A is applied after the change
    scenarios:
      - scenario: the owner picks A
        given: the fork is decided for A
        when: the plan is cut
        then: A is applied
        expected_from: literal
