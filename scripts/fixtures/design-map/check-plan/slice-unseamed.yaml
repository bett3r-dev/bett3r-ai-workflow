# The unit named its seam; this slice does not say it tests there.
seams:
  - name: the launcher's verdict line
    at: plugins/bett3r-ai-workflow/bin/design-map:1
    kind: existing
ticket: ESAS-9
review: unattended
slices:
  - id: 1
    name: "slice one"
    oracle: |
      the launcher prints outcome=ok and exits 0
    scenarios:
      - scenario: a slice names no seam
        given: a plan whose seams block is present
        when: a slice carries no seam key
        then: the launcher refuses it, naming the slice
