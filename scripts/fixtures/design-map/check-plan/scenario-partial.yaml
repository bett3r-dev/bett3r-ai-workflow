ticket: ESAS-9
review: unattended
slices:
  - id: 1
    name: "slice one"
    oracle: |
      the launcher prints outcome=ok and exits 0
    scenarios:
      - scenario: the owner picks A
        given: the fork is decided for A
        when: the plan is cut
