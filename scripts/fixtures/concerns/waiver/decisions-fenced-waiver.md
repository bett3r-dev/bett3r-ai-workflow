## D1 — The waiver entry shape is documented with an example
kind: deviation
step: build · slice: 1 · decidedBy: executor
sources: [code:commands/build.md]
rejected: —
supersedes: —
An example of a waiver entry, fenced, so it is prose and not an entry:

```markdown
## D7 — The owner waives C1: fenced example
kind: waiver
step: verify-build · slice: — · decidedBy: human
sources: [human]
rejected: —
supersedes: —
"an example quote, not a waiver"
```
