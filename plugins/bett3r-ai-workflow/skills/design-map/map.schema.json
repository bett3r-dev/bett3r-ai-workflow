{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ESAS map vocabulary",
  "description": "The closed sets an ESAS impact map is written in. Emitted from @bett3r-dev/esas-schema; do not hand-edit, and do not extend a set in a consumer.",
  "schemaVersion": 2,
  "$defs": {
    "forkStatusKind": {
      "type": "string",
      "description": "Where a fork stands.",
      "enum": [
        "open",
        "decided",
        "moot"
      ]
    },
    "decidedSource": {
      "type": "string",
      "description": "Who settled a decided fork.",
      "enum": [
        "owner",
        "recommendation",
        "code"
      ]
    },
    "mapNodeLevel": {
      "type": "string",
      "description": "A node's level in the impact map; the who-level is spelled mapActor.",
      "enum": [
        "goal",
        "mapActor",
        "impact",
        "deliverable"
      ]
    },
    "mapShape": {
      "type": "string",
      "description": "How a map is drawn: impact columns or a decision tree.",
      "enum": [
        "impact",
        "decision"
      ]
    }
  }
}
